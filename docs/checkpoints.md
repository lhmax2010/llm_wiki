# Checkpoints

> 每个已合并 Phase 的可回退锚点。回退命令会移动本地 `main`，执行前请确认没有未提交改动。

| tag | commit | 内容 | 回退命令 | 回退说明 |
|-----|--------|------|----------|----------|
| `checkpoint/phase_1_content_core` | `3751bcea2a18f3d1449e8433822d782e0eac58ec` | Phase 1 内容核、Pydantic schema、证据映射、SQLite 发号、code_binding shape-only 校验。PR #1，四路 review/R14 闭环，`48 passed`，coverage `96.84%`。 | `git switch main && git reset --hard checkpoint/phase_1_content_core` | 回到 Phase 1 合并点；后续 Phase 改动会从本地工作树消失。 |
| `checkpoint/phase_2_governed_api` | `b0a54ed72cf062d407753f429424eaf30b61492e` | Phase 2 Governed API middleware pipeline、RBAC、audit、persist 复用 Phase 1 core。PR #2，三路 review/R14 修复 role/diff/id/audit 信任边界，`85 passed`，coverage `95.49%`。 | `git switch main && git reset --hard checkpoint/phase_2_governed_api` | 回到 Phase 2 合并点；后续 Phase 改动会从本地工作树消失。 |
| `checkpoint/phase_3_mcp_server` | `e8c5ce90935d17202b31fbd148b54e4a350137d3` | Phase 3 MCP server 7 工具，propose 复用 Phase 2 pipeline，search/get_entry 物理隔离 research。PR #3，三路 review/R14 修复 path traversal 和 propose_update 发号绕过，`104 passed`，coverage `93.07%`。 | `git switch main && git reset --hard checkpoint/phase_3_mcp_server` | 回到 Phase 3 合并点；后续 Phase 改动会从本地工作树消失。 |
| `checkpoint/phase_4_index_search` | `8c33dbd9faf6d3c9057d821ace70d7c09517b5f6` | Phase 4 索引检索层，同义词、CJK bigram、min_support、agent/human path catalog、research_index placeholder、MCP search 索引切换 fallback。PR #4，三路 review/R14 修复 symlink 与坏 trust_state 绕过，`120 passed`，coverage `93.56%`。 | `git switch main && git reset --hard checkpoint/phase_4_index_search` | 回到 Phase 4 合并点；后续 Phase 改动会从本地工作树消失。 |
| `checkpoint/phase_5_staging_review` | `7a26423165904fe79dbd83edd475ff4660a15524` | Phase 5 staging review 发布闸门，queue、approve/reject、light/heavy RBAC、audit 留痕、reject 到 deprecated。PR #5，三路 review/R14 修复终态互斥和目录 symlink 两个 BLOCKER，`139 passed`，coverage `92.72%`。 | `git switch main && git reset --hard checkpoint/phase_5_staging_review` | 回到 Phase 5 合并点；后续 Phase 改动会从本地工作树消失。 |
| `checkpoint/phase_6_research_isolation` | `f31e3efe2f799740db79e12b776772c924d738fa` | Phase 6 research 物理隔离，独立 research index、opt-in hints、promote to draft、TTL report、research evidence 禁用。PR #7，三路 review/R14 修复 URI 洗证据 BLOCKER + 4 个 MAJOR，`170 passed`，coverage `91.36%`。 | `git switch main && git reset --hard checkpoint/phase_6_research_isolation` | 回到 Phase 6 合并点；保留 research 隔离和 R14 修复。 |
| `checkpoint/phase_10a_skill_contracts` | `486a9f9ede37b335dc9bbe6f2269b2c08ac49415` | Phase 10a skill 契约，正式化 `ingest_skill` / `maintenance_skill`，新增 validator。PR #6，三路 review 确认 skill 措辞范本，R14 修复 validator 3 个 MAJOR 绕过，`148 passed`，coverage `92.57%`。 | `git switch main && git reset --hard checkpoint/phase_10a_skill_contracts` | 回到 Phase 10a 合并点；后续 Phase 改动会从本地工作树消失。 |
| `checkpoint/phase_7a_web_readonly` | `5cc0828ae05d471cb5b4a669dc46f0b60d33f934` | Phase 7a Web 只读，FastAPI read-only API + React minimal frontend。PR #8，三路 review/Codex HTTP 实测零 BLOCKER/MAJOR，R14 修复错误信息路径泄漏 MINOR，`183 passed`，coverage `91.68%`。 | `git switch main && git reset --hard checkpoint/phase_7a_web_readonly` | 回到 Phase 7a 合并点；后续 Phase 改动会从本地工作树消失。 |
| `checkpoint/phase_8_web_edit` | `73e406e4725f71281de89e8a387cdda2488639d4` | Phase 8 Web 编辑，HTTP 写入只作为 P2 governed pipeline 入口，React minimal create/edit。PR #9，三路 review + Codex HTTP 写入实测，R14 修复发号 rebuild BLOCKER 与 P5 update republish MAJOR，`202 passed`，coverage `92.01%`。 | `git switch main && git reset --hard checkpoint/phase_8_web_edit` | 回到 Phase 8 合并点；后续 Phase 改动会从本地工作树消失。 |
